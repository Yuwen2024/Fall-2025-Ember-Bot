## First Time Setup

python3 -m venv venv 
source venv/bin/activate
pip3 install -r requirements.txt

## After Setup 

- Activate virtual environment: source venv/bin/activate
- Run the backend application: flask --app server run --host=0.0.0.0 --port=8000 --debug
